<%= form_for(@book_search_history) do |f| %>
  <% if @book_search_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_search_history.errors.count, "error") %> prohibited this book_search_history from being saved:</h2>

      <ul>
      <% @book_search_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_name %><br />
    <%= f.text_field :book_name %>
  </div>
  <div class="field">
    <%= f.label :search_count %><br />
    <%= f.text_field :search_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
